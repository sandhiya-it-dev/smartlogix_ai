# Cloud deployment - intentionally deferred

Per the current project scope, AWS EC2, RDS, S3, Lambda, and email notifications are not implemented. A later phase can containerize the API/dashboard, migrate SQLite to RDS PostgreSQL, store CV assets in S3, and trigger notifications from shipment events. No cloud credentials or placeholder integrations are included.

